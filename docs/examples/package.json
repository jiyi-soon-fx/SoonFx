{
  "name": "seer-engine-test",
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "dev": "npm run build && vite",
    "build": "vite build",
    "build:minify": "vite build --minify",
    "preview": "vite preview",
    "type-check": "tsc --noEmit"
  },
  "dependencies": {
    "@soonfx/engine": "2.0.0"
  },
  "devDependencies": {
    "@types/node": "^20.0.0",
    "typescript": "^5.0.0",
    "vite": "^5.0.0",
    "terser": "^5.24.0"
  }
}
